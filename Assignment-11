Question 16: How to Show this result based on the given details:
True-answers=6
Wrong -answers=0
Total-questions=6
Hint 1:- Use imageview and textview, containers for showing the image and text.
Hint 2:- Add the logic for showing the result based on true, and false values out of total questions.

source code:
import 'package:flutter/material.dart';

class ResultScreen extends StatelessWidget {
  final int trueAnswers;
  final int wrongAnswers;
  final int totalQuestions;

  const ResultScreen({
    Key? key,
    required this.trueAnswers,
    required this.wrongAnswers,
    required this.totalQuestions,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    double percentage = (trueAnswers / totalQuestions) * 100;

    return Scaffold(
      appBar: AppBar(
        title: const Text('Quiz Result'),
        centerTitle: true,
      ),
      body: Padding(
        padding: const EdgeInsets.all(16),
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            // 📊 Score Summary
            Row(
              mainAxisAlignment: MainAxisAlignment.spaceEvenly,
              children: [
                _buildInfoBox('Total Questions', totalQuestions, Colors.blue),
                _buildInfoBox('True Answers', trueAnswers, Colors.green),
                _buildInfoBox('Wrong Answers', wrongAnswers, Colors.red),
              ],
            ),
            const SizedBox(height: 40),

            // ✅ Percentage Circle
            CircleAvatar(
              radius: 50,
              backgroundColor: Colors.green,
              child: Text(
                '${percentage.toStringAsFixed(2)}%',
                style: const TextStyle(
                  color: Colors.white,
                  fontSize: 20,
                  fontWeight: FontWeight.bold,
                ),
              ),
            ),
            const SizedBox(height: 10),
            Text(
              percentage >= 50 ? 'Passed' : 'Failed',
              style: TextStyle(
                fontSize: 22,
                color: percentage >= 50 ? Colors.green : Colors.red,
                fontWeight: FontWeight.bold,
              ),
            ),
            const SizedBox(height: 30),

            // 🏆 Image / Trophy
            Image.asset(
              'assets/trophy.png', // place your image in assets folder
              height: 150,
            ),

            const SizedBox(height: 10),
            const Text(
              'Congratulations! You are passed.',
              style: TextStyle(fontSize: 18),
            ),
          ],
        ),
      ),
    );
  }

  Widget _buildInfoBox(String label, int value, Color color) {
    return Column(
      children: [
        Container(
          padding: const EdgeInsets.all(12),
          decoration: BoxDecoration(
            color: color,
            borderRadius: BorderRadius.circular(8),
          ),
          child: Text(
            value.toString(),
            style: const TextStyle(
              fontSize: 20,
              color: Colors.white,
              fontWeight: FontWeight.bold,
            ),
          ),
        ),
        const SizedBox(height: 6),
        Text(
          label,
          style: const TextStyle(fontSize: 14),
        ),
      ],
    );
  }
}

