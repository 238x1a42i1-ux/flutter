Question 23: You are working on a Content-based application based on a flutter, client wants you to show a random lucky number on the click button. Which is the max limit is 350. What things will you require to achieve this Problem statement?
Hint 1:- Add the method for generating random lucky numbers.
Hint 2:- Add text and an Elevated button widget.
source code:
import 'package:flutter/material.dart';
import 'dart:math'; // for generating random numbers

void main() {
  runApp(MaterialApp(
    debugShowCheckedModeBanner: false,
    home: LuckyNumberApp(),
  ));
}

class LuckyNumberApp extends StatefulWidget {
  @override
  _LuckyNumberAppState createState() => _LuckyNumberAppState();
}

class _LuckyNumberAppState extends State<LuckyNumberApp> {
  int? _luckyNumber; // To store generated number
  final Random _random = Random();

  // Method to generate a random number (1 to 350)
  void _generateLuckyNumber() {
    setState(() {
      _luckyNumber = _random.nextInt(350) + 1; // nextInt(350) gives 0–349
    });
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Lucky Number Generator")),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            Text(
              _luckyNumber == null
                  ? "Click the button to get your lucky number!"
                  : "🎉 Your Lucky Number is: $_luckyNumber 🎉",
              textAlign: TextAlign.center,
              style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold),
            ),
            SizedBox(height: 40),
            ElevatedButton(
              onPressed: _generateLuckyNumber,
              child: Text(
                "Generate Lucky Number",
                style: TextStyle(fontSize: 20),
              ),
            ),
          ],
        ),
      ),
    );
  }
}


