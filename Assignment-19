Question 24: You have two buttons A & B . When you click on the A button the image will be shown and you click on the B button then your name, phone number, and email will be shown. How will you achieve this problem statement?
Hint 1:- Add the flag to show the different-2 data on click of buttons.
Hint 2:- Add text in the column for showing the name, phone number, and email.
Hint 3:- Add imageView to show the image.
Hint 4:- Add the image to the assets folder.
source code:
import 'package:flutter/material.dart';

void main() {
  runApp(MaterialApp(
    debugShowCheckedModeBanner: false,
    home: ImageAndInfoToggle(),
  ));
}

class ImageAndInfoToggle extends StatefulWidget {
  @override
  _ImageAndInfoToggleState createState() => _ImageAndInfoToggleState();
}

class _ImageAndInfoToggleState extends State<ImageAndInfoToggle> {
  bool showImage = false; // flag to control what is displayed

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Toggle Image and Info Example")),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            // Display area based on flag
            if (showImage)
              Image.asset(
                'assets/my_image.png',
                width: 200,
                height: 200,
                fit: BoxFit.cover,
              )
            else
              Column(
                children: [
                  Text(
                    "Name: John Doe",
                    style: TextStyle(fontSize: 22, fontWeight: FontWeight.bold),
                  ),
                  Text(
                    "Phone: +1 9876543210",
                    style: TextStyle(fontSize: 20),
                  ),
                  Text(
                    "Email: johndoe@example.com",
                    style: TextStyle(fontSize: 20),
                  ),
                ],
              ),

            const SizedBox(height: 40),

            // Buttons Row
            Row(
              mainAxisAlignment: MainAxisAlignment.center,
              children: [
                ElevatedButton(
                  onPressed: () {
                    setState(() {
                      showImage = true; // show image
                    });
                  },
                  child: Text("Button A (Show Image)"),
                ),
                const SizedBox(width: 20),
                ElevatedButton(
                  onPressed: () {
                    setState(() {
                      showImage = false; // show text info
                    });
                  },
                  child: Text("Button B (Show Info)"),
                ),
              ],
            ),
          ],
        ),
      ),
    );
  }
}


