Question 4: Show a gif in UI that is based on a timer. If the timer is equal and less than 15 seconds then show the gif else display a message like running time has been over. How will you solve this problem statement? 
Hint 1:- Use futur.delayed for the duration
Hint 2:- Use a flag based on time and add logic to rendering the gif and text.
source code:
import 'package:flutter/material.dart';

void main() {
  runApp(GifTimerApp());
}

class GifTimerApp extends StatefulWidget {
  @override
  _GifTimerAppState createState() => _GifTimerAppState();
}

class _GifTimerAppState extends State<GifTimerApp> {
  bool showGif = true;

  @override
  void initState() {
    super.initState();

    Future.delayed(Duration(seconds: 15), () {
      setState(() {
        showGif = false; // Time over, hide GIF
      });
    });
  }

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'GIF Timer Demo',
      home: Scaffold(
        appBar: AppBar(
          title: Text('GIF Timer Example'),
        ),
        body: Center(
          child: showGif
              ? Image.network(
                  'https://media.giphy.com/media/3oEjI6SIIHBdRxXI40/giphy.gif',
                  width: 200,
                  height: 200,
                  fit: BoxFit.cover,
                )
              : Text(
                  'Running time has been over.',
                  style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold),
                ),
        ),
      ),
    );
  }
}
