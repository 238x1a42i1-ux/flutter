Question 5: How to move a widget from the top left corner to the right bottom corner by clicking on a button?
Hint 1:- Use an image widget or container.
Hint 2:- Use Align widget for moving.
source code:
import 'package:flutter/material.dart';

void main() {
  runApp(MoveWidgetApp());
}

class MoveWidgetApp extends StatefulWidget {
  @override
  _MoveWidgetAppState createState() => _MoveWidgetAppState();
}

class _MoveWidgetAppState extends State<MoveWidgetApp> {
  Alignment _alignment = Alignment.topLeft;

  void _moveWidget() {
    setState(() {
      _alignment = _alignment == Alignment.topLeft
          ? Alignment.bottomRight
          : Alignment.topLeft;
    });
  }

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Move Widget Demo',
      home: Scaffold(
        appBar: AppBar(
          title: Text('Move Widget on Button Click'),
        ),
        body: Stack(
          children: [
            Align(
              alignment: _alignment,
              child: Container(
                width: 100,
                height: 100,
                color: Colors.blue,
                child: Center(
                    child: Text(
                  'Move me',
                  style: TextStyle(color: Colors.white),
                )),
              ),
            ),
          ],
        ),
        floatingActionButton: FloatingActionButton(
          onPressed: _moveWidget,
          child: Icon(Icons.play_arrow),
          tooltip: 'Move Widget',
        ),
      ),
    );
  }
}
